<<: !include extensions.yaml

dependencies:
  - aeson
  - name: base
    mixin:
      - hiding (Prelude)
  - containers
  - generics-sop
  - lens
  - polysemy
  - text
  - type-errors
  - type-errors-pretty

ghc-options:
  - -Wall
  - -Wredundant-constraints
  - -Wsimplifiable-class-constraints
