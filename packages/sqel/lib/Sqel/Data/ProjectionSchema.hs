module Sqel.Data.ProjectionSchema where

data ProjectionSchema proj table =
  ProjectionSchema

-- data ProjectionSchema proj table =
--   ProjectionSchema {
--     column :: [Selector],
--     decoder :: Row proj
--   }
--   deriving stock (Generic)
